# Streamlit Community Cloud: https://share.streamlit.io
# Repo: inezaodon/image-quality-cnn · Branch: main · Main file: streamlit_app.py
#
# Local:  python3 -m streamlit run streamlit_app.py
#
# Redeploys: pushing to the connected branch rebuilds the SAME public URL.
# PDFs are embedded from local repo files inside the app (not GitHub blob pages).
